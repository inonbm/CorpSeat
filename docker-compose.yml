services:
  web:
    build: .
    container_name: corpseat_web
    ports:
      - "5000:5000"
    volumes:
      - corpseat_data:/app/data
    environment:
      - FLASK_ENV=production
      - DB_PATH=/app/data/corpseat.db
    restart: unless-stopped

volumes:
  corpseat_data:
